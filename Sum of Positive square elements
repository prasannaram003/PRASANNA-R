QUESTION:

Write a C program to find the sum of positive square elements in an array:
Input & Output Format:

First Input consists of size of an array

Remaining inputs corresponds to the number of elements based on the size.

Output consists of an integer.

Sample Input:
3
2
4
6

Sample Output:
4

CODING:

#include<iostream>
using namespace std;
int main()
{
    int a,sum=0;
    cin>>a;
    int b[a];
    for(int i=0;i<a;i++)
    {
        cin>>b[i];
        if((b[i]==1)||(b[i]==4)||(b[i]==9)||(b[i]==16)||(b[i]==25))
    {
        sum=sum+b[i];
    }
}
cout<<sum;
}
